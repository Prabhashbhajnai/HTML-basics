Git

1. initialized --> git init                                         (Only do one time in each project)
2. git config --global user.email "prabhashbhajani@gmail.com"       (This step 2 is only for first time setup)
   git config --global user.name "Prabhashbhajani"                  
3. git add .                                                        (Staging)
4. git commmit -m ("Message in quotation")
5. git remote add origin (link to repo)                             (To connect to git server) ((Only do one time in each project))
6. git push origin master                                            (Push code to git hub)

For rest of the time:

3. git add .
4. git commmit -m ("Message in quotation")
6. git push origin master